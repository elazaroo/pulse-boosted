providers:
  - Elazaroo\PulseBoosted\PulseServiceProvider

migrations:
  - database/migrations

workbench:
  start: '/pulse-boosted'
  install: true
  welcome: false
  build:
    - asset-publish
    - create-sqlite-db
    - migrate-refresh
