providers:
  - Elazaroo\PulseBoosted\PulseServiceProvider
  - Workbench\App\Providers\WorkbenchServiceProvider

migrations:
  - database/migrations

workbench:
  start: '/pulse-boosted'
  install: true
  welcome: false
  build:
    - asset-publish
    - create-sqlite-db
    - migrate-refresh
